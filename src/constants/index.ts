import { IPaginationParams } from '@/types/services';

export const API_URL = 'https://www.breakingbadapi.com/api';
export const BASE_PAGINATION_PARAMS: IPaginationParams = {
  limit: 10,
  offset: 10,
};
